import { Post, PostSchema, PostDocument } from './post.schema';
import { Category, CategorySchema, CategoryDocument } from './category.schema';
import { Comment, CommentSchema, CommentDocument } from './comment.schema';
import { User, UserSchema, UserDocument } from './user.schema';

export { PostDocument, CategoryDocument, CommentDocument, UserDocument };
export { Post, Category, Comment, User };
export { PostSchema, CategorySchema, CommentSchema, UserSchema };
